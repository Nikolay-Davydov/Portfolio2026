/** @type {import('next').NextConfig} */
module.exports = {
  images: {
    domains: [],
  },
  i18n: {
    locales: ["en", "ru"],
    defaultLocale: "en",
  },
};
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
